# R library installation

Definition of sets of R libraries that should be installed along with an R
function to install them.

- [install_packages.R](install_packages.R): provides the `install_packages()`
  function to install the R libraries.
- R libraries to install should be added to *txt* file(s), one line per R
  package.
